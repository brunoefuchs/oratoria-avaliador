# Story 7.1: Report — Bug Fixes + Renomeacoes PT-BR

## Status
Ready

## Size
S (estimado ~7h total em 7 tasks)

## Business Value
Corrige 4 bugs matematicos + 11 strings tecnicas em ingles que impactam diretamente a percepcao de precisao do relatorio — dor #1 elicitada pelo PM na criacao do Epic 7 ("quero a analise mais precisa tecnicamente possivel"). Sem esta story, as stories seguintes (7.2-7.5) vao somar novos cards sobre uma base ainda com erros visiveis.

## Executor Assignment
- executor: @dev
- consult: —
- quality_gate: @qa

## Epic
Epic 7 — Relatorio: Precisao Tecnica Maxima (`docs/stories/7.0.epic-report-precision.md`)

## Story

**As a** colaborador da empresa se auto-avaliando pelo oratoria-avaliador,
**I want** que os scores do relatorio sejam matematicamente corretos, nao duplicados, e usem termos claros em portugues brasileiro (nao jargao tecnico em ingles),
**so that** confio na nota que recebo e entendo o que cada metrica significa sem precisar consultar um dicionario tecnico.

## Contexto

Handoff de analise tecnica (`docs/sessions/2026-04/handoff-vinh-report-technical-gaps.md`) identificou 4 bugs de calculo + 11 strings a renomear + 1 vicio nao detectado ('ne'). Sao fixes de baixo risco e alto valor percebido — precisao e legibilidade.

Screenshots de referencia: `ajustes oratoria/1.png` a `10.png` (anotacoes do usuario em caneta azul).

## Acceptance Criteria

### Bugs de Calculo

1. **AC-1 Diversidade de arquetipos correta**: Quando o orador usa apenas 1 arquetipo em 100% do video, o sub-score `Diversidade` em `Arquetipos Vocais` deve ser **0** (nao 25 como atualmente).
   - Se usa 4 arquetipos igualmente distribuidos (25% cada) → Diversidade = 100
   - Se usa 2 arquetipos em 50/50 → Diversidade = ~50
   - Formula: `(archetypes_used / 4) * evenness * 100`, onde `evenness` usa entropia de Shannon (ou equivalente)
   - Evidencia atual: `ajustes oratoria/8.png` mostra 1 arquetipo usado + Diversidade 25

2. **AC-2 Fillers/min e Muletas/min consolidados**: Substituir os dois campos duplicados por um unico campo `vicios_linguagem_por_minuto`.
   - Remover do UI: `Fillers por minuto` e `Muletas por minuto`
   - Adicionar: `Vicios de Linguagem por Minuto` (soma dos dois)
   - Label atualizada: "Meta: < 3/min"
   - Evidencia: `ajustes oratoria/7.png` anotacao "FILLERS POR MINUTO e MULETAS POR MINUTO sao a mesma coisa e podem ser substituidos"

3. **AC-3 Contato visual com banda ideal**: Score de contato visual nao deve tratar 100% como ideal. Banda ideal: 70-90%.
   - 100% contato continuo → score ~50 (penalizado como "staring")
   - 85% → score 100
   - 40% → score ~57
   - 0% → score 0
   - Curva bell (sino) com pico entre 70-90%
   - UI label atualizada: "Zona ideal: 70-90%"
   - Evidencia: `ajustes oratoria/5.png` user anota questionamento sobre contato visual 100

4. **AC-4 Gesticulacao score bidirecional**: Score de gesticulacao nao deve ser unidirecional. Pouca gesticulacao E excesso sao ruins.
   - Banda ideal a definir via calibracao inicial (sugestao: 60-80 gestos variados/min)
   - Score 100 dentro da banda, degrada para ambos os lados
   - UI diferencia visualmente "Pouca variacao" vs "Excesso / Distrai"
   - Evidencia: `ajustes oratoria/3.png` e `6.png` user questiona ambiguidade

### Renomeacoes PT-BR (11 strings)

5. **AC-5 Termos traduzidos e padronizados**: Trocar as seguintes strings em toda a UI do report (`apps/web/src/app/report/[id]/**`):

   | Atual | Trocar por |
   |-------|-----------|
   | PROSÓDIA / Prosodia | DICCAO |
   | DEFAULT (como default detectado) | PADRAO / PADROES |
   | DIMENSOES TRAVADAS | PILARES TRAVADOS |
   | PITCH | TOM |
   | SCORE (topo) | PONTUACAO |
   | WPM | PALAVRAS POR MINUTO |
   | GROUNDING | ESTABILIDADE CORPORAL |
   | Fillers | Vicios de Linguagem |
   | Cluster de Fillers | Problemas de Fluencia |
   | Diversidade Lexical | Riqueza de Vocabulario |
   | Fillers por minuto + Muletas por minuto | Vicios de Linguagem por Minuto (item AC-2) |

### Deteccao de Vicios

6. **AC-6 Deteccao do 'ne'**: Adicionar 'ne' / 'né' a lista de vicios detectados pelo `filler_detector.py`.
   - Reconhecer variantes: `ne`, `né`, `ne?`
   - Testado com video de referencia que contem 1 ocorrencia nao detectada hoje
   - Evidencia: `ajustes oratoria/7.png` user anota "faltou identificar o uso do 'ne'"

### Regressao Zero

7. **AC-7 Zero regressao em videos processados anteriormente**: Reprocessar 3 videos ja avaliados e comparar:
   - Score geral pode mudar pouco (pela banda ideal de eye contact), mas UI renderiza sem erro
   - Historico no Supabase continua legivel
   - Nenhum campo removido quebra consulta

## Scope

### IN

**Backend (ml-worker/workers):**
- `archetype_classifier.py` — nova formula de diversidade (AC-1)
- `filler_detector.py` — consolidar fillers+muletas + adicionar 'ne' (AC-2, AC-6)
- `gesture_analyzer.py` — banda ideal eye contact + gesticulacao bidirecional (AC-3, AC-4)

**Frontend (apps/web/src/app/report):**
- `[id]/page.tsx` — renomeacoes gerais
- `[id]/[dimension]/page.tsx` — renomeacoes por dimensao + remover campos duplicados
- `components/score-card.tsx` (se aplicavel) — labels atualizadas

**Testes:**
- Unit tests novos/atualizados em `ml-worker/tests/` para os 4 bugs
- Snapshot de relatorio reprocessado para validar AC-7

### OUT

- Nao introduzir novos scores/dimensoes (sao outras stories do Epic 7)
- Nao mexer em `congruence_analyzer`, `opening_analyzer`, `identity_analyzer` (outras stories)
- Nao fazer refactor do `report_generator.py` (flag de risco no Epic 7, nao e esta story)
- Nao alterar schema Supabase se possivel (campos consolidados podem ser calculados em runtime)
- Nao mexer em logica de pesos do score geral (story 7.2)

## Analise Tecnica

### AC-1 — Formula de diversidade de arquetipos

```python
# archetype_classifier.py
import numpy as np

def compute_diversity(distribution: dict[str, float]) -> float:
    """
    distribution: {"educator": 0.0, "coach": 0.0, "motivator": 0.0, "friend": 1.0}
    retorna: 0-100
    """
    archetypes_used = sum(1 for v in distribution.values() if v > 0.05)

    if archetypes_used <= 1:
        return 0.0

    # Shannon entropy normalized
    values = [v for v in distribution.values() if v > 0]
    entropy = -sum(v * np.log(v) for v in values)
    max_entropy = np.log(4)  # max when perfectly distributed
    evenness = entropy / max_entropy if max_entropy > 0 else 0

    return round((archetypes_used / 4) * evenness * 100, 1)
```

Teste esperado: `compute_diversity({"friend": 1.0, ...zeros}) == 0.0`

### AC-2 — Consolidacao fillers + muletas

Duas opcoes:
- **Opcao A** (recomendada): calcular em runtime no `filler_detector.py` ou `aggregator.py`, mantendo campos brutos no `detailed_metrics` para rastreabilidade, mas expondo no UI apenas `vicios_linguagem_por_minuto`.
- **Opcao B**: migration no Supabase removendo campos — mais invasivo, descartado para esta story.

Usar opcao A. Backend mantem `fillers_per_minute` e `muletas_per_minute` internamente + adiciona `vicios_per_minute = fillers + muletas`.

### AC-3 — Curva bell de eye contact

```python
def eye_contact_score(percent: float) -> float:
    if 70 <= percent <= 90:
        return 100.0
    elif percent < 70:
        return (percent / 70.0) * 100
    else:  # > 90
        return max(0.0, 100 - (percent - 90) * 5)
```

Ideal band 70-90 com decay 5 pontos por % acima de 90.

### AC-4 — Banda ideal gesticulacao

Precisa calibracao. Sugerir valores iniciais baseados em videos existentes:
- Baixo (<20 gestos/min): pouca variacao → score 30-60
- Ideal (60-80 gestos/min): score 90-100
- Alto (>100 gestos/min): excesso → score 50-70

Definir constantes no topo de `gesture_analyzer.py` com comentario "calibrado em 2026-04 com video de referencia — ajustar se feedback user divergir".

### AC-6 — 'ne' no filler_detector

Adicionar regex `\b(ne|né)\b(\s*\??)?` com boundary word. Garantir que captura variacoes com/sem acento e com/sem `?` apos.

### AC-5 — Renomeacoes

Provavel uso de `src/i18n/pt.json` ou constantes em `lib/labels.ts` — se nao existe, criar arquivo de labels centralizado nesta story (mini-refactor), evitando grep-replace disperso no codigo.

Sugestao: criar `apps/web/src/lib/report-labels.ts` com:
```typescript
export const REPORT_LABELS = {
  dimensions: {
    voice_prosody: "Voz e Diccao",
    ...
  },
  metrics: {
    pitch: "Tom",
    wpm: "Palavras por Minuto",
    ...
  }
};
```

E importar nos componentes. Trabalho de ~1h de organizacao que paga no 7.2 em diante.

## Tasks / Subtasks

- [ ] **Task 1: Bug AC-1 — Diversidade de arquetipos** (esforco: 1h)
  - [ ] Atualizar `compute_diversity` em `ml-worker/workers/archetype_classifier.py`
  - [ ] Adicionar testes em `ml-worker/tests/test_archetype_classifier.py` (casos: 1 arquetipo, 2 em 50/50, 4 iguais)
  - [ ] Reprocessar video de referencia (8.png) e verificar Diversidade = 0

- [ ] **Task 2: Bug AC-2 + AC-6 — Fillers consolidados + 'ne'** (esforco: 1h)
  - [ ] Adicionar `vicios_per_minute` calculado em `ml-worker/workers/filler_detector.py` ou `aggregator.py`
  - [ ] Adicionar regex para 'ne'/'né' em filler_detector
  - [ ] Testes para 'ne' + consolidacao
  - [ ] Atualizar UI para mostrar 1 campo

- [ ] **Task 3: Bug AC-3 — Eye contact banda ideal** (esforco: 45min)
  - [ ] Atualizar `eye_contact_score` em `ml-worker/workers/gesture_analyzer.py`
  - [ ] Testes com valores 0, 40, 70, 85, 90, 95, 100
  - [ ] Atualizar label UI "Zona ideal: 70-90%"

- [ ] **Task 4: Bug AC-4 — Gesticulacao bidirecional** (esforco: 1h)
  - [ ] Definir constantes `IDEAL_GESTURES_MIN`, `IDEAL_GESTURES_MAX` em gesture_analyzer.py
  - [ ] Nova formula com decay bidirecional
  - [ ] UI diferencia "Pouca variacao" / "Ideal" / "Excesso"
  - [ ] Testes

- [ ] **Task 5: Renomeacoes AC-5** (esforco: 2h)
  - [ ] Criar `apps/web/src/lib/report-labels.ts` com todas as strings
  - [ ] Substituir strings em `report/[id]/page.tsx`
  - [ ] Substituir strings em `report/[id]/[dimension]/page.tsx`
  - [ ] Busca por strings hardcoded remanescentes: "prosody", "fillers", "WPM", "grounding", "pitch"
  - [ ] Smoke test visual em /report/{id} e cada dimensao

- [ ] **Task 6: Regressao AC-7** (esforco: 30min)
  - [ ] Reprocessar 3 videos antigos ja avaliados
  - [ ] Comparar relatorios antigo vs novo (score geral pode mudar pouco)
  - [ ] Validar que UI renderiza sem erros
  - [ ] Validar que consultas no Supabase continuam funcionando

- [ ] **Task 7: CodeRabbit + Tests + Done** (esforco: 30min)
  - [ ] Rodar `pytest ml-worker/tests/` (todos passam)
  - [ ] Rodar `npm run lint` + `npm run type-check` em apps/web
  - [ ] CodeRabbit self-healing (max 2 iteracoes)
  - [ ] Atualizar File List
  - [ ] Mover status Ready for Review

## Dev Notes

- **Backup:** esta story NAO altera schema Supabase. Se quiser extra seguranca, criar branch `feature/7.1-report-bug-fixes-renames` antes de comecar.
- **Calibracao AC-4:** se os valores 60-80 gestos/min nao parecerem certos no primeiro teste, ajustar e documentar na task 4.
- **Ordem sugerida de execucao:** Task 1 → 2 → 3 → 4 → 5 → 6 → 7 (serie, cada uma independente ~45min-2h).
- **Handoff origem:** `docs/sessions/2026-04/handoff-vinh-report-technical-gaps.md` secoes P0 BUG #1-4 e Renomeacoes.

## Riscos e Mitigacoes

| Risco | Impacto | Mitigacao |
|-------|---------|-----------|
| Task 5 (renomeacoes) quebra visual ou a11y de algum componente | Medio | Fazer por ultimo + smoke test obrigatorio em cada dimensao; criar `lib/report-labels.ts` centralizado facilita rollback |
| Videos antigos ja no Supabase mantem valores antigos (ex: `diversity: 25` persistido) | Baixo | Mudanca nao e retroativa por design — so afeta novos processamentos. Documentar em release notes |
| AC-4 calibracao gesticulacao errada (valores 60-80 sao chute inicial) | Medio | Primeira iteracao ja testa em video real; ajustar constantes no mesmo PR se feedback diverge |
| CodeRabbit flaga falso positivo em divisao por zero de eye_contact_score | Baixo | Adicionar guard explicito `if percent < 0: return 0` mesmo sendo impossivel |
| Renomeacoes quebram grep/busca do usuario por termo antigo (ex: "Prosódia") | Baixo | Termos sao internos da UI — devs usam campo tecnico no backend. Documentar no CHANGELOG |

## CodeRabbit Integration

- **Enabled:** true
- **Mode:** light self-healing (max 2 iteracoes, CRITICAL issues only)
- **Pre-commit trigger:** antes de mover Ready for Review
- **Expected issues:** potencial warning em `eye_contact_score` (divisao por zero se percent < 0 — impossivel mas CodeRabbit pode flagar)

## Testing

### Unit tests (ml-worker)
- `test_archetype_classifier.py::test_diversity_single_archetype` → 0.0
- `test_archetype_classifier.py::test_diversity_even_four` → 100.0
- `test_archetype_classifier.py::test_diversity_two_equal` → ~50.0
- `test_filler_detector.py::test_detects_ne_variations` → captura 'ne', 'né', 'ne?'
- `test_gesture_analyzer.py::test_eye_contact_bell_curve` → 100 quando 85%, ~50 quando 100%, 0 quando 0%
- `test_gesture_analyzer.py::test_gesticulation_bidirectional` → ideal=100, pouco=~50, muito=~60

### Integration test
- Reprocessar video `ajustes oratoria/1.png` referencia (score antigo: 45) e comparar:
  - Arquetipos Vocais: diversidade vai de 25 → 0 (mas score dimensao pode cair proporcionalmente)
  - Clareza Verbal: campo consolidado aparece
  - Presenca Visual: eye contact 100 sai de "perfect" para "penalizado"

### Manual smoke test
- Abrir relatorio no browser, navegar por todas as 6 dimensoes, verificar renomeacoes visuais

## File List
(Dev preenche durante implementacao)

## Change Log

| Data | Autor | Mudanca |
|------|-------|---------|
| 2026-04-14 | @sm River | Story criada a partir do Epic 7 + handoff Vinh |
| 2026-04-14 | @po Pax | Validacao 10-point: 8.5/10 GO. Adicionado Size S, Business Value, tabela de Riscos. Status Draft → Ready |
