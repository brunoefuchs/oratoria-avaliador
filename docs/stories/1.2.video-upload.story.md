# Story 1.2: Video Upload UI & Storage

## Status
Ready

## Executor Assignment
- executor: @dev
- quality_gate: @architect
- quality_gate_tools: [lint, typecheck, test]

## Story

**As a** user,
**I want** to upload a video file through the web interface,
**so that** the system can process it for oratory evaluation.

## Acceptance Criteria

1. Página de upload com drag-and-drop zone e botão de seleção de arquivo
2. Validação client-side: aceita MP4/WebM, max 500MB, max 10 min de duração
3. Exibe mensagem de erro clara para arquivos inválidos (formato, tamanho, duração)
4. Upload faz stream para Supabase Storage via API backend
5. Após upload bem-sucedido, cria registro na tabela `evaluations` com status `uploaded`
6. UI exibe progresso de upload (% concluído)
7. Após upload, redireciona para tela de processamento com o ID da avaliação

## CodeRabbit Integration

> **CodeRabbit Integration**: Disabled — Greenfield project, CodeRabbit não configurado.

## Tasks / Subtasks

- [ ] Task 1: Criar migration do schema initial (AC: 5)
  - [ ] Criar `supabase/migrations/001_initial_schema.sql` com tabela `evaluations`
  - [ ] Criar bucket `videos` no Supabase Storage (via migration ou manual)
  - [ ] Aplicar migration: `supabase db push` ou via dashboard
- [ ] Task 2: Criar endpoint de upload no backend API (AC: 4, 5)
  - [ ] Criar `apps/api/app/routes/evaluations.py`
  - [ ] Implementar `POST /evaluations` que recebe `multipart/form-data`
  - [ ] Validar arquivo server-side (formato, tamanho)
  - [ ] Upload para Supabase Storage bucket `videos`
  - [ ] Inserir registro em `evaluations` com status `uploaded`
  - [ ] Retornar `201 {id, status, video_url, created_at}`
- [ ] Task 3: Criar modelos Pydantic (AC: 5)
  - [ ] Criar `apps/api/app/models/evaluation.py` com schemas de request/response
  - [ ] Criar `apps/api/app/repositories/evaluation_repo.py` com CRUD básico
- [ ] Task 4: Criar componente VideoUploader no frontend (AC: 1, 2, 3, 6)
  - [ ] Criar `apps/web/src/components/video-uploader.tsx`
  - [ ] Implementar drag-and-drop zone com visual feedback
  - [ ] Validação client-side: formato (MP4/WebM), tamanho (500MB), duração (10 min)
  - [ ] Verificar duração via `HTMLVideoElement` antes do upload
  - [ ] Exibir mensagens de erro claras por tipo de validação
  - [ ] Exibir progress bar durante upload (fetch com upload progress)
- [ ] Task 5: Criar página de upload (AC: 1, 7)
  - [ ] Criar `apps/web/src/app/page.tsx` com componente VideoUploader
  - [ ] Criar `apps/web/src/lib/api-client.ts` com fetch wrapper para backend
  - [ ] Após upload com sucesso, redirecionar para `/processing/[id]`
- [ ] Task 6: Testes unitários (AC: 2, 3)
  - [ ] Teste backend: `POST /evaluations` com arquivo válido e inválido
  - [ ] Teste frontend: Componente VideoUploader renderiza, valida formatos

## Dev Notes

### Database Schema — tabela evaluations
[Source: architecture.md#database-schema]

```sql
CREATE TABLE evaluations (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    video_url TEXT NOT NULL,
    status TEXT NOT NULL DEFAULT 'uploaded'
        CHECK (status IN ('uploaded', 'processing', 'analyzed', 'completed', 'error')),
    substatus TEXT,
    duration_seconds INTEGER,
    error_message TEXT,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    completed_at TIMESTAMPTZ
);

CREATE INDEX idx_evaluations_status ON evaluations(status);
CREATE INDEX idx_evaluations_created ON evaluations(created_at DESC);
```

### API Endpoint
[Source: architecture.md#api-specification]

- `POST /evaluations` — multipart/form-data com campo `video`
- Response 201: `{id, status: "uploaded", video_url, created_at}`
- Error 400: `{code: "INVALID_VIDEO_FORMAT|VIDEO_TOO_LARGE|VIDEO_TOO_LONG", message: "..."}`

### Frontend — Estrutura de Arquivos
[Source: architecture.md#unified-project-structure]

```
apps/web/src/
├── app/
│   └── page.tsx                    # Landing/Upload page
├── components/
│   └── video-uploader.tsx          # Drag-and-drop upload component
├── lib/
│   ├── api-client.ts               # Fetch wrapper para backend
│   └── utils.ts
└── types/
    └── index.ts                    # TypeScript interfaces
```

### Validação de Duração no Browser

```typescript
// Verificar duração antes do upload
const video = document.createElement('video');
video.preload = 'metadata';
video.onloadedmetadata = () => {
  if (video.duration > 600) { // 10 min = 600s
    setError('Vídeo deve ter no máximo 10 minutos');
  }
};
video.src = URL.createObjectURL(file);
```

### Supabase Storage

- Bucket: `videos`
- Path pattern: `evaluations/{evaluation_id}/video.{ext}`
- Limpar após processamento (story futura)

### Testing
- Backend: pytest + httpx TestClient para testar endpoints
- Frontend: Vitest + @testing-library/react para componentes
- Fixtures: criar arquivo de vídeo mock (pequeno MP4) para testes

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-30 | 0.1 | Story criada — YOLO mode | @sm (River) |

## Dev Agent Record

### Agent Model Used
_To be filled by dev agent_

### Debug Log References
_To be filled by dev agent_

### Completion Notes List
_To be filled by dev agent_

### File List
_To be filled by dev agent_

## QA Results
_To be filled by QA agent_
