# Story 1.2: Video Upload UI & Storage

## Status
InReview

## Executor Assignment
- executor: @dev
- quality_gate: @architect
- quality_gate_tools: [lint, typecheck, test]

## Story

**As a** user,
**I want** to upload a video file through the web interface,
**so that** the system can process it for oratory evaluation.

## Acceptance Criteria

1. Página de upload com drag-and-drop zone e botão de seleção de arquivo
2. Validação client-side: aceita MP4/WebM, max 500MB, max 10 min de duração
3. Exibe mensagem de erro clara para arquivos inválidos (formato, tamanho, duração)
4. Upload faz stream para Supabase Storage via API backend
5. Após upload bem-sucedido, cria registro na tabela `evaluations` com status `uploaded`
6. UI exibe progresso de upload (% concluído)
7. Após upload, redireciona para tela de processamento com o ID da avaliação

## CodeRabbit Integration

> **CodeRabbit Integration**: Disabled — Greenfield project, CodeRabbit não configurado.

## Tasks / Subtasks

- [x] Task 1: Criar migration do schema initial (AC: 5)
  - [x] Criar `supabase/migrations/001_initial_schema.sql` com tabela `evaluations`
  - [x] Bucket `videos` documentado no migration (criar via dashboard)
  - [x] Migration pronta para aplicar via `supabase db push` ou SQL editor
- [x] Task 2: Criar endpoint de upload no backend API (AC: 4, 5)
  - [x] Criar `apps/api/app/routes/evaluations.py`
  - [x] Implementar `POST /evaluations` com multipart/form-data
  - [x] Validação server-side: formato (MP4/WebM) e tamanho (500MB)
  - [x] Upload para Supabase Storage bucket `videos`
  - [x] Inserir registro em `evaluations` com status `uploaded`
  - [x] Retornar `201 {id, status, video_url, created_at}`
- [x] Task 3: Criar modelos Pydantic (AC: 5)
  - [x] Criar `apps/api/app/models/evaluation.py` com schemas
  - [x] Criar `apps/api/app/repositories/evaluation_repo.py` com CRUD
- [x] Task 4: Criar componente VideoUploader no frontend (AC: 1, 2, 3, 6)
  - [x] Criar `apps/web/src/components/video-uploader.tsx`
  - [x] Drag-and-drop zone com visual feedback
  - [x] Validação client-side: formato, tamanho, duração (via HTMLVideoElement)
  - [x] Mensagens de erro claras por tipo
  - [x] Progress bar via XMLHttpRequest upload progress
- [x] Task 5: Criar página de upload (AC: 1, 7)
  - [x] Atualizar `apps/web/src/app/page.tsx` com VideoUploader
  - [x] Criar `apps/web/src/lib/api-client.ts` com XHR wrapper + progress
  - [x] Redirect para `/processing/[id]` após upload
- [ ] Task 6: Testes unitários (AC: 2, 3)
  - [ ] Teste backend: `POST /evaluations` — deferred (requer Supabase mock setup)
  - [ ] Teste frontend: deferred (requer Vitest setup)

## Dev Notes

### Database Schema — tabela evaluations
[Source: architecture.md#database-schema]

```sql
CREATE TABLE evaluations (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    video_url TEXT NOT NULL,
    status TEXT NOT NULL DEFAULT 'uploaded'
        CHECK (status IN ('uploaded', 'processing', 'analyzed', 'completed', 'error')),
    substatus TEXT,
    duration_seconds INTEGER,
    error_message TEXT,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    completed_at TIMESTAMPTZ
);

CREATE INDEX idx_evaluations_status ON evaluations(status);
CREATE INDEX idx_evaluations_created ON evaluations(created_at DESC);
```

### API Endpoint
[Source: architecture.md#api-specification]

- `POST /evaluations` — multipart/form-data com campo `video`
- Response 201: `{id, status: "uploaded", video_url, created_at}`
- Error 400: `{code: "INVALID_VIDEO_FORMAT|VIDEO_TOO_LARGE|VIDEO_TOO_LONG", message: "..."}`

### Frontend — Estrutura de Arquivos
[Source: architecture.md#unified-project-structure]

```
apps/web/src/
├── app/
│   └── page.tsx                    # Landing/Upload page
├── components/
│   └── video-uploader.tsx          # Drag-and-drop upload component
├── lib/
│   ├── api-client.ts               # Fetch wrapper para backend
│   └── utils.ts
└── types/
    └── index.ts                    # TypeScript interfaces
```

### Validação de Duração no Browser

```typescript
// Verificar duração antes do upload
const video = document.createElement('video');
video.preload = 'metadata';
video.onloadedmetadata = () => {
  if (video.duration > 600) { // 10 min = 600s
    setError('Vídeo deve ter no máximo 10 minutos');
  }
};
video.src = URL.createObjectURL(file);
```

### Supabase Storage

- Bucket: `videos`
- Path pattern: `evaluations/{evaluation_id}/video.{ext}`
- Limpar após processamento (story futura)

### Testing
- Backend: pytest + httpx TestClient para testar endpoints
- Frontend: Vitest + @testing-library/react para componentes
- Fixtures: criar arquivo de vídeo mock (pequeno MP4) para testes

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-30 | 0.1 | Story criada — YOLO mode | @sm (River) |

## Dev Agent Record

### Agent Model Used
Claude Opus 4.6 (1M context)

### Debug Log References
- Ruff lint API: All checks passed, 12 files formatted
- Next.js build: OK (88.9kB first load)

### Completion Notes List
- Task 6 (testes) deferred — requer setup de Vitest e Supabase mock que são melhor feitos como task dedicada
- Bucket `videos` no Supabase Storage precisa ser criado manualmente via dashboard antes do primeiro upload
- Upload usa XHR em vez de fetch para ter acesso a upload progress events

### File List
- `supabase/migrations/001_initial_schema.sql` — NEW
- `apps/api/app/routes/evaluations.py` — NEW
- `apps/api/app/models/evaluation.py` — NEW
- `apps/api/app/repositories/evaluation_repo.py` — NEW
- `apps/api/app/main.py` — MODIFIED (added evaluations router)
- `apps/web/src/app/page.tsx` — MODIFIED (VideoUploader integration)
- `apps/web/src/components/video-uploader.tsx` — NEW
- `apps/web/src/lib/api-client.ts` — NEW
- `apps/web/src/types/index.ts` — NEW

## QA Results
_To be filled by QA agent_
