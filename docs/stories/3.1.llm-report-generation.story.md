# Story 3.1: LLM Report Generation

## Status
Ready

## Executor Assignment
- executor: @dev
- quality_gate: @qa
- quality_gate_tools: [pytest, ruff]

## Story

**As a** system,
**I want** to generate a qualitative feedback report from the aggregated metrics using Gemini 2.0 Flash,
**so that** the user receives human-readable, actionable insights about their oratory.

## Acceptance Criteria

1. Prompt estruturado envia metricas agregadas para Gemini 2.0 Flash via `google-generativeai` SDK
2. Prompt inclui: scores por dimensao, metricas detalhadas, contexto sobre oratoria, instrucoes de formato
3. LLM retorna: resumo geral (2-3 frases), feedback por dimensao (pontos fortes + pontos a melhorar + dica pratica), score qualitativo (ex: "Bom", "Precisa atencao")
4. Resposta do LLM parseada e armazenada em JSON estruturado na tabela `reports`
5. Status da avaliacao atualizado para `completed`
6. Em caso de erro na API do LLM, retry com backoff exponencial (max 3 tentativas) antes de marcar como `error`
7. Custo da chamada LLM logado para monitoramento de custo por avaliacao

## CodeRabbit Integration

> **CodeRabbit Integration**: Disabled -- CodeRabbit nao configurado.

## Dependencies

- **Story 2.5** (Metrics Aggregation): aggregated_metrics JSON payload disponivel
- **Gemini API Key**: `GEMINI_API_KEY` configurada no ml-worker

## Tasks / Subtasks

- [ ] Task 1: Criar migration SQL para tabela `reports` (AC: 4)
  - [ ] Criar `supabase/migrations/YYYYMMDD_create_reports.sql`
  - [ ] Validar constraints (UNIQUE evaluation_id, NOT NULL fields)
- [ ] Task 2: Implementar prompt builder (AC: 1, 2)
  - [ ] Criar `ml-worker/workers/report_generator.py`
  - [ ] Implementar `build_prompt(aggregated_metrics: dict) -> str`
  - [ ] Estruturar prompt com: role system (especialista em oratoria), metricas, instrucoes de formato JSON
  - [ ] Incluir contexto: faixas de referencia por metrica, escala qualitativa
- [ ] Task 3: Integrar Gemini 2.0 Flash via google-generativeai SDK (AC: 1, 7)
  - [ ] Configurar `google.generativeai` com `GEMINI_API_KEY`
  - [ ] Implementar `call_gemini(prompt: str) -> str` com model `gemini-2.0-flash`
  - [ ] Logar tokens de input/output para estimativa de custo
  - [ ] Registrar `llm_model` e `llm_cost_usd` na resposta
- [ ] Task 4: Implementar retry com backoff exponencial (AC: 6)
  - [ ] Implementar decorator/wrapper com retry (max 3, backoff 1s -> 2s -> 4s)
  - [ ] Tratar erros especificos: rate limit (429), server error (500+), timeout
  - [ ] Apos 3 falhas, marcar avaliacao como `error` com mensagem descritiva
- [ ] Task 5: Parsear e validar resposta do LLM (AC: 3, 4)
  - [ ] Implementar `parse_report_response(raw: str) -> ReportData`
  - [ ] Validar estrutura: summary (str), dimension_feedback (dict com 4 dimensoes), qualitative labels
  - [ ] Fallback: se JSON invalido, tentar extracoes parciais antes de falhar
- [ ] Task 6: Salvar report e atualizar status (AC: 4, 5)
  - [ ] Salvar em tabela `reports` via Supabase client
  - [ ] Atualizar status da avaliacao para `completed` via callback API
  - [ ] Se report generation falhou, status = `error`
- [ ] Task 7: Integrar no pipeline do ml-worker (AC: 1-7)
  - [ ] Adicionar step de report generation apos aggregation em `ml-worker/workers/pipeline.py`
  - [ ] Substatus: `generating_report`
  - [ ] Atualizar `orchestrate_pipeline()` para chamar report generator
- [ ] Task 8: Testes unitarios
  - [ ] Testar prompt builder com metricas completas e incompletas
  - [ ] Testar parse de resposta valida e invalida
  - [ ] Testar retry logic (mock API failures)
  - [ ] Testar fluxo completo com mock Gemini

## Dev Notes

### DB Schema -- reports
[Source: PRD Epic 3, Story 3.1]

```sql
CREATE TABLE reports (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    evaluation_id UUID NOT NULL REFERENCES evaluations(id) ON DELETE CASCADE UNIQUE,
    summary TEXT NOT NULL,
    dimension_feedback JSONB NOT NULL,
    llm_model TEXT NOT NULL,
    llm_cost_usd NUMERIC(10, 6) NOT NULL DEFAULT 0,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
```

### Gemini 2.0 Flash Integration
[Source: architecture.md#tech-stack]

- SDK: `google-generativeai` (Python)
- Model: `gemini-2.0-flash` (free tier)
- Configuracao: `GEMINI_API_KEY` env var no ml-worker
- Cost tracking: free tier, mas logar tokens para monitoramento

```python
import google.generativeai as genai

genai.configure(api_key=os.getenv("GEMINI_API_KEY"))
model = genai.GenerativeModel("gemini-2.0-flash")

response = model.generate_content(prompt)
report_text = response.text
```

### Prompt Structure

```python
SYSTEM_PROMPT = """Voce e um especialista em oratoria e comunicacao.
Analise as metricas de uma apresentacao e gere um relatorio qualitativo em PT-BR.

Formato de resposta (JSON):
{
  "summary": "Resumo geral em 2-3 frases",
  "dimension_feedback": {
    "voice": {
      "strengths": ["ponto forte 1", "ponto forte 2"],
      "improvements": ["melhoria 1", "melhoria 2"],
      "tip": "Dica pratica especifica",
      "qualitative_label": "Bom|Excelente|Precisa atencao|Critico"
    },
    "fillers": { ... },
    "posture": { ... },
    "gesture": { ... }
  }
}
"""
```

### Retry with Exponential Backoff

```python
import asyncio
from functools import wraps

async def retry_with_backoff(func, max_retries=3, base_delay=1.0):
    for attempt in range(max_retries):
        try:
            return await func()
        except Exception as e:
            if attempt == max_retries - 1:
                raise
            delay = base_delay * (2 ** attempt)
            logger.warning(f"Gemini API attempt {attempt + 1} failed: {e}. Retrying in {delay}s")
            await asyncio.sleep(delay)
```

### Report JSON Format (dimension_feedback)

```json
{
  "summary": "Sua apresentacao demonstra boa projecao vocal e postura confiante. Os principais pontos de melhoria sao a reducao de vicios de linguagem e maior variacao de gestos.",
  "dimension_feedback": {
    "voice": {
      "strengths": ["Ritmo de fala adequado (145 WPM)", "Boa projecao vocal"],
      "improvements": ["Aumentar variacao de pitch para mais dinamismo"],
      "tip": "Pratique variando o tom ao enfatizar pontos-chave da apresentacao.",
      "qualitative_label": "Bom"
    },
    "fillers": {
      "strengths": ["Diversidade lexical razoavel"],
      "improvements": ["Reduzir uso de 'ne' (5 ocorrencias)", "4.8 fillers/min esta acima do ideal"],
      "tip": "Faca pausas silenciosas de 1-2 segundos ao inves de preencher com 'ne' ou 'tipo'.",
      "qualitative_label": "Precisa atencao"
    },
    "posture": {
      "strengths": ["Score de alinhamento alto (82)", "Boa estabilidade corporal"],
      "improvements": ["Aumentar % de postura aberta (atualmente 75.5%)"],
      "tip": "Mantenha os ombros abertos e evite cruzar os bracos ao responder perguntas.",
      "qualitative_label": "Bom"
    },
    "gesture": {
      "strengths": ["Bom contato visual (78.3%)"],
      "improvements": ["Aumentar gesticulacao ativa (62.5%)", "Usar mais gestos acima da cintura"],
      "tip": "Use gestos abertos na altura do peito para enfatizar argumentos importantes.",
      "qualitative_label": "Precisa atencao"
    }
  }
}
```

### File Locations
- Report generator: `ml-worker/workers/report_generator.py`
- Pipeline integration: `ml-worker/workers/pipeline.py` (add report step)
- Migration: `supabase/migrations/YYYYMMDD_create_reports.sql`
- Status callback: `POST /evaluations/{id}/status-update` com `{"status": "completed"}`

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-30 | 0.1 | Story criada para Epic 3 | @sm (River) |

## Dev Agent Record

### Agent Model Used
_To be filled by dev agent_

### Debug Log References
_To be filled by dev agent_

### Completion Notes List
_To be filled by dev agent_

### File List
_To be filled by dev agent_

## QA Results
_To be filled by QA agent_
