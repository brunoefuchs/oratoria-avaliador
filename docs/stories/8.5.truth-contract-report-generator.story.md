# Story 8.5: Truth Contract — Report Generator Rewrite

## Status
InReview

## Size
L (~12h em 6 tasks)

## Business Value

Ultima story do Epic 8. Fecha os 10 vetos do audit original do report_generator.

Hoje, mesmo com 8.1-8.4 done, o report_generator:
- Recebe `aggregated: dict` sem contrato (V8)
- Substitui `score=None` por `0` no prompt ("Pontuacao: 0/100") → LLM gera coaching em cima de mentira (V9)
- Aceita identity com score=50 fake (V10) — mesmo ja corrigido no worker, report ainda usa `identity.get("score")`
- Markdown stripping fragil quebra JSON parsing (V11)
- Retry 3x com prompt identico (V12)
- Retrocompat silenciosa `summary + resumo` (V13)
- `_format_metrics` esconde dados (V14), `_rank_problems` + `_build_cross_insights` com keys hardcoded (V15, V16)
- Identity check vs Opening check inconsistentes (V17)

**Apos esta story:** 24/24 vetos fechados. Pipeline honesto ponta a ponta.

## Depends On
- Story 8.4 Done (aggregator retorna `overall_score: int | None` + `partial_aggregation`)

## Executor Assignment
- **executor:** @dev (Dex) — modo Pre-Flight
- **quality_gate:** @qa (Quinn)
- **push:** @devops (Gage)

---

## Acceptance Criteria

### AC1 — generate_report recebe AggregatedMetrics tipado (não dict)
```
Given aggregator retorna dict com overall_score: int | None + partial_aggregation
When generate_report eh chamado
Then aceita AggregatedMetrics (Pydantic) ou dict validado por TypeAdapter
And campos obrigatorios validados antes de chamar Gemini
```

### AC2 — Score None vira texto explicito no prompt (não "0/100")
```
Given overall_score=None ou dimension_scores tem faltantes
When prompt eh construido
Then prompt diz "Pontuacao: indisponivel (X de Y dimensoes falharam)" NAO "0/100"
And dimensoes faltantes aparecem explicitamente
And LLM NAO gera coaching em cima de scores ausentes
```

### AC3 — Identity check usa dimension_status, não score=50
```
Given identity agora retorna WorkerFailure quando falha (nao score=50)
When report avalia se inclui identity no prompt
Then usa isinstance(identity, WorkerSuccess) ou identity.dimension_status == "ok"
And NAO usa identity.get("score") != 50
And WorkerFailure de identity → prompt omite secao identidade
```

### AC4 — Retry com variacao
```
Given primeira tentativa falhou (JSONDecodeError)
When retry dispara
Then temperature varia entre tentativas (0.7 → 0.5 → 0.3)
And prompt adiciona instrucao "retorne APENAS JSON valido"
And log registra qual temp foi usada
```

### AC5 — Retrocompat removida
```
Given report novo gerado
When salvo no banco
Then retorna apenas schema novo (resumo, dimensoes) — nao mais summary + dimension_feedback
And consumers downstream (frontend) ja migrados pro schema novo
And llm_cost_usd real (nao 0.0 fake)
```

### AC6 — Feature flag dispatch
```
Given TRUTH_CONTRACT_ENABLED=true
When app.py chama report generator
Then chama generate_report() novo (Pydantic input)

Given flag=false
Then chama generate_report_legacy() preservado
```

---

## Tasks

### T1 — Criar AggregatedMetrics Pydantic model
- [x] `contracts/aggregated_metrics.py`:
  - `AggregatedMetrics` BaseModel com: `overall_score: int | None`, `dimension_scores: dict[str, int]`, `detailed_metrics: dict`, `incomplete_dimensions: list[str]`, `partial_aggregation: bool`, `contexto: str | None`, `pesos_utilizados: dict[str, float]`, `video_metadata: dict`, augmentation fields (identity, opening, storytelling, temporal, congruence como Optional[WorkerResult])
- [x] Export em `contracts/__init__.py`
- [x] Tests: validation happy/failure paths

### T2 — Reescrever generate_report pra aceitar AggregatedMetrics
- [x] Nova signature: `generate_report(metrics: AggregatedMetrics, context: dict | None = None) -> dict`
- [x] Validar input via Pydantic antes de qualquer coisa
- [x] Extrair prompt builder em funcao pura: `_build_prompt(metrics: AggregatedMetrics, context) -> str`
- [x] Prompt builder testavel sem chamar Gemini

### T3 — Prompt honesto pra scores ausentes
- [x] Se `overall_score is None`: prompt diz "Pontuacao geral: INDISPONIVEL — X de Y dimensoes falharam"
- [x] Se `dim_score is None` (dimensao faltou): prompt diz "Dimensao X: INDISPONIVEL" NAO "Dimensao X: 0/100"
- [x] Guard rails atualizados: `incomplete_dimensions` vira lista explicita pro LLM
- [x] Remover `_format_metrics` com `.get(x, 0)` silencioso — logar quando esconde
- [x] Veto T3.1: `grep '", 0)' report_generator.py` retorna 0 matches em paths do prompt

### T4 — Identity/Opening checks via WorkerResult
- [x] Identity check: `dimension_status == "ok"` em vez de `identity.get("score") != 50`
- [x] Opening check: mesmo pattern consistente
- [x] `_build_cross_insights` + `_rank_problems`: usar `detailed_metrics` validado (nao dict.get hardcoded)
- [x] Veto T4.1: nenhuma referencia a `score != 50` ou `score > 0` pra identity

### T5 — Gemini JSON mode + retry com variacao
- [x] Configurar `response_mime_type="application/json"` na chamada Gemini
- [x] Remover markdown stripping fragil (nao precisa mais)
- [x] Retry loop: 3 tentativas, temperature [0.7, 0.5, 0.3]
- [x] Log `llm_cost_usd` real (calcular via token usage)
- [x] Se 3 retries falham: raise → app.py trata como report indisponivel (nao grava report parcial)

### T6 — App.py wire + regression
- [x] Flag ON path: chama `generate_report(metrics)` novo
- [x] Flag OFF path: preserva `generate_report_legacy(aggregated: dict)` atual
- [x] Remover campos retrocompat do retorno (summary, dimension_feedback)
- [x] Regression: todos 191 tests PASS + 34 novos tests 8.5 = 225 total PASS
- [x] Veto T6.1: report novo nao tem `llm_cost_usd: 0.0` fake

---

## Veto Conditions de Story

| # | Condicao | Validacao |
|---|----------|-----------|
| V1 | `generate_report` recebe Pydantic, nao dict | grep signature |
| V2 | Score None vira "INDISPONIVEL" no prompt, nao "0/100" | Test prompt builder |
| V3 | Identity check usa isinstance ou dimension_status | grep |
| V4 | Gemini JSON mode configurado | grep response_mime_type |
| V5 | Retry varia temperature | Test |
| V6 | Retrocompat fields removidos | grep "summary.*resumo" |
| V7 | Todos tests PASS (191+ novos) | CI |

---

## Dev Notes

### Prompt Honesto

```python
# ANTES (V9 veto):
prompt += f"Pontuacao: {dimension_scores.get('variety', 0)}/100"

# DEPOIS:
variety_score = dimension_scores.get('variety')
if variety_score is None:
    prompt += "Variedade: INDISPONIVEL (analise falhou)"
else:
    prompt += f"Variedade: {variety_score}/100"
```

### Gemini JSON Mode

```python
import google.generativeai as genai

model = genai.GenerativeModel("gemini-2.5-flash")
response = model.generate_content(
    prompt,
    generation_config={
        "response_mime_type": "application/json",
        "response_schema": _REPORT_SCHEMA,
        "temperature": temp,
    }
)
# response.text eh JSON garantido — sem markdown stripping
report = json.loads(response.text)
```

---

## File List

- `ml-worker/contracts/aggregated_metrics.py` — NEW: AggregatedMetrics Pydantic model
- `ml-worker/contracts/__init__.py` — MOD: export AggregatedMetrics
- `ml-worker/workers/report_generator.py` — REWRITE: generate_report TC path + generate_report_legacy preserved
- `ml-worker/app.py` — MOD: Step 10 feature flag dispatch
- `ml-worker/tests/test_report_generator_truth_contract.py` — NEW: 34 tests, 225 total pass

## Change Log

| Data | Versao | Mudanca | Autor |
|------|--------|---------|-------|
| 2026-04-15 | 0.1 | Draft baseado em V8-V17 do audit report_generator. Pydantic input, JSON mode, retry variacao, retrocompat remove. | pedro-valerio |
| 2026-04-15 | 1.0 | Implementation completa: T1-T6 done. 34 novos tests. 225/225 pass. Syntax fix: escaped quotes em f-strings Python 3.12. | @dev |
