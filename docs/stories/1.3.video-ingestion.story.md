# Story 1.3: Video Ingestion & Audio/Video Split

## Status
Ready

## Executor Assignment
- executor: @dev
- quality_gate: @architect
- quality_gate_tools: [lint, test]

## Story

**As a** system,
**I want** to receive an uploaded video and split it into separate audio and video streams,
**so that** each analysis engine can process its relevant stream independently.

## Acceptance Criteria

1. API leve (Render) dispara processamento no ML worker (HF Spaces) via HTTP POST com `evaluation_id` e `video_url`
2. ML worker baixa o vídeo do Supabase Storage
3. FFmpeg extrai áudio (WAV 16kHz mono) e mantém referência ao vídeo original
4. Arquivos processados ficam no filesystem temporário do ML worker
5. Status da avaliação atualizado para `processing` com substatus `splitting`
6. Em caso de erro no FFmpeg ou download, status atualizado para `error` com mensagem descritiva
7. Logging estruturado registra início, duração e resultado do split
8. Endpoint `GET /evaluations/{id}/status` retorna status atual com substatus

## CodeRabbit Integration

> **CodeRabbit Integration**: Disabled — Greenfield project, CodeRabbit não configurado.

## Tasks / Subtasks

- [ ] Task 1: Implementar dispatcher na API leve (AC: 1)
  - [ ] Criar `apps/api/app/services/dispatcher.py`
  - [ ] Implementar `dispatch_to_ml_worker(evaluation_id, video_url)` — HTTP POST para ML worker
  - [ ] Incluir `CALLBACK_SECRET` no header para autenticação
  - [ ] Fire-and-forget: não esperar resposta do worker
  - [ ] Atualizar status para `processing` após dispatch
- [ ] Task 2: Implementar endpoint de status (AC: 8)
  - [ ] Adicionar `GET /evaluations/{id}/status` em `evaluations.py`
  - [ ] Retornar `{id, status, substatus, progress: {steps_completed, steps_total, current_step}}`
- [ ] Task 3: Implementar recebimento no ML worker (AC: 2)
  - [ ] Implementar `POST /process` em `ml-worker/app.py`
  - [ ] Validar `CALLBACK_SECRET` do header
  - [ ] Baixar vídeo do Supabase Storage para `/tmp/`
  - [ ] Iniciar pipeline em background (asyncio)
- [ ] Task 4: Implementar video processor com FFmpeg (AC: 3, 4)
  - [ ] Criar `ml-worker/workers/video_processor.py`
  - [ ] `split_video(video_path) → (audio_path, video_path)` usando subprocess + FFmpeg
  - [ ] Extrair áudio: `ffmpeg -i input.mp4 -ar 16000 -ac 1 -f wav output.wav`
  - [ ] Manter vídeo original no path original
  - [ ] Cleanup de temp files após processamento completo
- [ ] Task 5: Implementar callback para API (AC: 5, 6)
  - [ ] Criar `apps/api/app/routes/callback.py`
  - [ ] `POST /evaluations/{id}/callback` — recebe resultados do ML worker
  - [ ] Validar `CALLBACK_SECRET`
  - [ ] Atualizar status no banco (via callback para cada etapa)
  - [ ] ML worker faz callback de status update: `POST /evaluations/{id}/status-update {substatus}`
- [ ] Task 6: Logging estruturado (AC: 7)
  - [ ] Configurar structlog no ML worker (`ml-worker/utils/logging.py`)
  - [ ] Log: início do processamento, duração do download, duração do split, erros
- [ ] Task 7: Testes (AC: 3, 6)
  - [ ] Teste: `split_video` com arquivo MP4 de teste (fixture)
  - [ ] Teste: `POST /process` com payload válido e inválido
  - [ ] Teste: `GET /evaluations/{id}/status` retorna status correto

## Dev Notes

### Fluxo de Comunicação API ↔ ML Worker
[Source: architecture.md#architectural-patterns]

```
API (Render) → POST /process {evaluation_id, video_url, callback_url} → ML Worker (HF Spaces)
ML Worker → POST /evaluations/{id}/status-update {substatus} → API (para cada etapa)
ML Worker → POST /evaluations/{id}/callback {results} → API (no final)
```

**Autenticação:** Header `X-Callback-Secret` com valor de `CALLBACK_SECRET` env var. Ambos os serviços compartilham o mesmo secret.

### FFmpeg Commands
[Source: architecture.md#components]

```bash
# Extrair áudio WAV 16kHz mono (formato ideal para Whisper)
ffmpeg -i input.mp4 -ar 16000 -ac 1 -f wav output.wav

# Extrair frames a 2fps (para stories futuras - pose estimation)
ffmpeg -i input.mp4 -vf fps=2 frame_%04d.jpg
```

### ML Worker — Endpoint POST /process

```python
# Request body
{
    "evaluation_id": "uuid",
    "video_url": "evaluations/uuid/video.mp4",  # Path no Supabase Storage
    "callback_url": "https://oratoria-api.onrender.com/evaluations/{id}/callback"
}
```

### API — Endpoint GET /evaluations/{id}/status
[Source: architecture.md#api-specification]

```json
{
  "id": "550e8400...",
  "status": "processing",
  "substatus": "splitting",
  "progress": {
    "steps_completed": 0,
    "steps_total": 6,
    "current_step": "Separando áudio e vídeo..."
  }
}
```

**Steps:** splitting → analyzing_posture → analyzing_gesture → analyzing_voice → analyzing_fillers → generating_report

### Error Handling
[Source: architecture.md#error-handling-strategy]

- FFmpeg falha → status `error`, error_message descritiva
- Download do vídeo falha → status `error`, error_message "Falha ao baixar vídeo"
- ML worker indisponível → API retorna status `error` após timeout (30s)

### Testing
- pytest para ML worker (`ml-worker/tests/`)
- Fixture: criar MP4 de teste pequeno (5s) com ffmpeg: `ffmpeg -f lavfi -i testsrc=duration=5:size=320x240:rate=24 -f lavfi -i sine=frequency=440:duration=5 test.mp4`

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-30 | 0.1 | Story criada — YOLO mode | @sm (River) |

## Dev Agent Record

### Agent Model Used
_To be filled by dev agent_

### Debug Log References
_To be filled by dev agent_

### Completion Notes List
_To be filled by dev agent_

### File List
_To be filled by dev agent_

## QA Results
_To be filled by QA agent_
