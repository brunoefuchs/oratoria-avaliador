# Story 7.4: Report — Facial Expression Analyzer

## Status
Draft

## Executor Assignment
- executor: @dev
- consult: @architect (MediaPipe Face Mesh tuning)
- quality_gate: @qa

## Epic
Epic 7 — Relatorio: Precisao Tecnica Maxima (`docs/stories/7.0.epic-report-precision.md`)

## Story

**As a** colaborador que se auto-avaliou,
**I want** que a avaliacao inclua a expressao facial (sorriso, movimento de sobrancelhas, variacao emocional no rosto),
**so that** recebo feedback nao apenas do meu corpo e voz, mas tambem do rosto — que representa 40% da percepcao emocional de quem me assiste.

## Contexto

Handoff Vinh (P2 #10) identificou gap: "Corpo e medido. Rosto nao. Smile/seriedade/cejas/micro-expressoes sao ~40% da percepcao emocional."

O ml-worker ja usa MediaPipe Pose e Hand. Esta story adiciona MediaPipe Face Mesh (Face Landmarker) ao mesmo runtime.

Stack atual: Python 3.12, MediaPipe ja instalado, pipeline roda em container Docker.

## Acceptance Criteria

### Metricas Faciais Core

1. **AC-1 Smile frequency**: % do tempo em que o rosto apresenta sorriso (leve ou intenso).
   - Usar landmarks do Face Mesh para calcular "smile index" (distancia canto-boca vs canto-rosto)
   - Threshold calibrado: smile ativo quando smile_index > 0.4
   - Output: `smile_frequency_percent: float`

2. **AC-2 Smile variability**: Variacao no nivel do sorriso ao longo do video.
   - Calcular desvio padrao do smile_index em janelas de 5s
   - Diferencia sorriso "travado" (stdev baixo, 100% do tempo ativo) de sorriso "natural" (stdev moderado)
   - Output: `smile_variability: float` (0-1)

3. **AC-3 Brow movement**: Movimento das sobrancelhas como sinal de engajamento / surpresa.
   - Detectar eventos de elevacao rapida de sobrancelhas (landmarks 105, 334)
   - Contar eventos por minuto
   - Output: `brow_raises_per_minute: float`

4. **AC-4 Eye openness variation**: Variacao na abertura dos olhos (sinal de atencao vs fadiga).
   - Calcular eye aspect ratio (EAR) ao longo do video
   - Output: `eye_openness_stddev: float`

5. **AC-5 Diagnostico agregado**: Score 0-100 e diagnostico textual:
   - `rosto_estatico` (stdev baixo em todas metricas)
   - `expressao_monotona` (smile travado ou ausente)
   - `expressao_equilibrada` (variacao saudavel)
   - `expressao_exagerada` (movimento excessivo, pode distrair)

### Output

6. **AC-6 Formato de output**: Campo `detailed_metrics.facial`:
   ```python
   {
     "disponivel": bool,
     "score": int,
     "diagnostico": str,
     "smile_frequency_percent": float,
     "smile_variability": float,
     "brow_raises_per_minute": float,
     "eye_openness_stddev": float,
     "emocional_texture": {
       "neutro_percent": float,
       "positivo_percent": float,
       "tenso_percent": float
     },
     "feedback": str,
     "warnings": list[str]  # ex: "rosto nao detectado em 30% do video"
   }
   ```

### Congruence Feed

7. **AC-7 Integracao com Congruence Score**: Enviar dados faciais para `congruence_analyzer.py` (ja existe) para cruzamento com voz/corpo.
   - Exemplo: se voz soa triste mas rosto sorri 100% do tempo → flag de incongruencia
   - Nao alterar a logica do congruence_analyzer nesta story — apenas expor os dados em `detailed_metrics.facial` para ele consumir

### Integracao

8. **AC-8 Integrado ao pipeline**: Worker chamado em `ml-worker/app.py` no Step 3.5 (entre gestos e voz — depois que pose/face ja foram processados pelo video_processor).

9. **AC-9 Surface no relatorio**: Card "Expressao Facial" em `/report/[id]/page.tsx` (nivel de sub-dimensao, junto com Identidade/Abertura/Narrativa).
   - Mostra: smile frequency, diagnostico, feedback
   - Drill-down fica para enhancement futuro

### Fallback

10. **AC-10 Video sem rosto detectavel**: Se MediaPipe nao detecta rosto em >50% dos frames, retornar `disponivel: false` com warning explicativo ("rosto nao visivel — possivel enquadramento, iluminacao ou oclusao").

## Scope

### IN

- `ml-worker/workers/facial_analyzer.py` — novo worker
- `ml-worker/workers/video_processor.py` — adicionar extracao de face landmarks (se nao existe)
- `ml-worker/requirements.txt` — mediapipe ja esta, nada novo se rodar offline
- `ml-worker/app.py` — Step 3.5 pipeline integration
- `apps/web/src/app/report/[id]/page.tsx` — card facial
- `apps/web/src/components/facial-card.tsx` — novo componente
- `ml-worker/tests/test_facial_analyzer.py` — 3+ videos de teste
- `congruence_analyzer.py` — consumir novo campo (read-only, nao refactor)

### OUT

- Reconhecimento de emocoes complexas (tristeza, raiva, medo) — fora do escopo local
- Deteccao de micro-expressoes (<200ms) — requer frame rate alto, out of scope
- Drill-down `/report/[id]/facial` — enhancement futuro
- Score facial entrar no overall_score — decisao separada (weight 0 inicialmente)
- Analise de gaze direction (MediaPipe Face ja detecta mas esta em `gesture_analyzer` — manter separado)

## Analise Tecnica

### Pipeline extension

```python
# ml-worker/workers/facial_analyzer.py
import mediapipe as mp
import numpy as np
from typing import TypedDict

face_mesh = mp.solutions.face_mesh.FaceMesh(
    max_num_faces=1,
    refine_landmarks=True,
    min_detection_confidence=0.5,
)

SMILE_LANDMARK_IDS = {"mouth_left": 61, "mouth_right": 291, "jaw_left": 172, "jaw_right": 397}
BROW_LANDMARK_IDS = {"left_brow": 105, "right_brow": 334, "left_eye_upper": 159, "right_eye_upper": 386}

def analyze_facial(video_path: str, fps: int = 10) -> dict:
    """Extract facial features at reduced fps (10/s suficiente, economiza compute)."""
    frames = extract_frames(video_path, fps=fps)
    metrics_per_frame = []

    for frame in frames:
        results = face_mesh.process(frame)
        if not results.multi_face_landmarks:
            metrics_per_frame.append(None)
            continue

        landmarks = results.multi_face_landmarks[0].landmark
        metrics_per_frame.append({
            "smile_index": compute_smile_index(landmarks),
            "brow_elevation": compute_brow_elevation(landmarks),
            "eye_openness_left": compute_eye_aspect_ratio(landmarks, "left"),
            "eye_openness_right": compute_eye_aspect_ratio(landmarks, "right"),
        })

    return aggregate_metrics(metrics_per_frame, fps=fps)
```

### Smile index formula (indicativa — validar no spike)

```python
def compute_smile_index(landmarks) -> float:
    # Distancia horizontal canto-boca
    mouth_width = distance(landmarks[61], landmarks[291])
    # Distancia entre cantos do maxilar
    jaw_width = distance(landmarks[172], landmarks[397])
    # Smile index normalizado
    return mouth_width / jaw_width if jaw_width > 0 else 0.0
```

Threshold: smile_index > 0.4 = sorriso ativo (calibrar em videos reais).

### Performance

- 10 fps e suficiente (rosto humano nao muda dramaticamente em <100ms)
- Video 5min = 3000 frames a processar → ~30s em CPU moderna
- Considerar batch processing se ficar lento

## Tasks / Subtasks

- [ ] **Task 0: Spike de 1h** ⚠️
  - [ ] Testar MediaPipe Face Mesh em 1 video existente
  - [ ] Validar smile_index formula contra sorrisos visiveis
  - [ ] Calibrar threshold 0.4 com 5 frames observados manualmente
  - [ ] Documentar thresholds em constantes no topo de facial_analyzer.py

- [ ] **Task 1: Setup do worker** (esforco: 1.5h)
  - [ ] Criar `ml-worker/workers/facial_analyzer.py`
  - [ ] Estrutura base + types
  - [ ] Unit tests de helpers (distance, compute_smile_index, eye_aspect_ratio)

- [ ] **Task 2: Smile frequency + variability** (esforco: 1.5h)
  - [ ] Agregacao em janelas de 5s
  - [ ] Calculo de stdev
  - [ ] Unit tests com videos de referencia (sorriso constante vs variado)

- [ ] **Task 3: Brow movement** (esforco: 1h)
  - [ ] Deteccao de eventos de elevacao (> threshold em < 0.5s)
  - [ ] Counter por minuto
  - [ ] Unit tests

- [ ] **Task 4: Eye openness variation** (esforco: 1h)
  - [ ] EAR calculation
  - [ ] Stdev em janelas
  - [ ] Unit tests

- [ ] **Task 5: Diagnostico agregado + feedback** (esforco: 1h)
  - [ ] Score formula combinando 4 metricas
  - [ ] Mapeamento para diagnosticos textuais
  - [ ] Sugestoes em linguagem natural

- [ ] **Task 6: Integracao pipeline** (esforco: 1h)
  - [ ] Step 3.5 em ml-worker/app.py
  - [ ] `detailed_metrics.facial` populado

- [ ] **Task 7: Congruence integration** (esforco: 30min)
  - [ ] Garantir que `congruence_analyzer.py` ve o novo campo
  - [ ] Sem alterar logica dele, apenas validar leitura

- [ ] **Task 8: UI card** (esforco: 2h)
  - [ ] `components/facial-card.tsx`
  - [ ] Integrar em report/[id]/page.tsx
  - [ ] Fallback se `disponivel: false`

- [ ] **Task 9: Validacao em 5 videos** (esforco: 1.5h)
  - [ ] 5 videos (rosto estatico, sorriso travado, expressivo, baixa luz, sem rosto)
  - [ ] Verificar diagnostico apropriado em cada
  - [ ] Ajustar thresholds se necessario

- [ ] **Task 10: Testes + CodeRabbit + Done** (esforco: 45min)
  - [ ] pytest ml-worker/tests/test_facial_analyzer.py
  - [ ] Lint frontend
  - [ ] CodeRabbit
  - [ ] File List
  - [ ] Ready for Review

## Dev Notes

- **MediaPipe Face Mesh ja disponivel no container** — nao precisa adicionar dependencia, apenas usar.
- **Performance** — 10fps e sweet spot. Se ficar lento, reduzir para 5fps (rosto nao e tao dinamico).
- **Thresholds sao calibrados** — documentar no codigo. Se usuario reportar falso positivo/negativo, ajustar e commitar.
- **Rosto nao detectado** — muito comum em videos mal enquadrados. Fallback e critico (AC-10).
- **Congruence** — facial_analyzer nao altera congruence, so fornece dados. congruence_analyzer pode evoluir em story separada.
- **Paralelizavel** com 7.3 e 7.5 (stories independentes, podem ir em worktrees paralelos).

## CodeRabbit Integration

- **Enabled:** true
- **Mode:** light self-healing
- **Expected issues:** divisao por zero em compute_smile_index, performance em loops sobre landmarks

## Testing

### Unit tests
- `test_smile_index_neutral_face` — face neutra retorna smile_index < 0.4
- `test_smile_index_smiling_face` — face com sorriso retorna > 0.4
- `test_brow_raise_detection` — detecta elevacao rapida
- `test_eye_openness_variation` — variacao em janelas
- `test_no_face_detected` — retorna `disponivel: false` com warning

### Integration
- 5 videos de calibracao com diagnosticos esperados anotados manualmente

### Manual smoke
- Relatorio com card facial completo
- Relatorio de video sem rosto mostra fallback gracioso

## File List
(Dev preenche)

## Change Log

| Data | Autor | Mudanca |
|------|-------|---------|
| 2026-04-14 | @sm River | Story draftada |
