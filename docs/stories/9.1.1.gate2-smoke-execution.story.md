# Story 9.1.1: Gate 2 Smoke Execution

## Status
Ready for Review

## Size
S (~4h)

## Business Value

Story 9.1 deferiu Gate 2 smoke execution. Protocolo pronto em `docs/qa/gate2-story91-smoke.md`, mas execução requer:
1. Script `replay_eval.py` (~30 linhas) — re-executa apenas `aggregate_metrics` sobre `detailed_metrics` salvo (sem re-processar vídeo)
2. Rodar em 5+ evals reais do Supabase
3. Preencher tabela Gate 2 + emitir veredito PASS/FAIL

**Ambiente:** Supabase tem 5 `completed` evals com `aggregated_metrics` + 13 dims salvas. Suficiente pra Gate 2 inicial (AC6 de 9.2 aceitou N≥3).

## Depends On
- Story 9.1 MERGED ✅

## Executor
- @dev (inline, auto mode)

---

## Acceptance Criteria

### AC1 — Script `replay_eval.py`
Lê `aggregated_metrics.detailed_metrics` de uma eval, reconstrói `WorkerSuccess` por dim, chama `aggregate_metrics` v0.7.0 vs v0.6.0 (via flag toggle), retorna tupla (score_v060, score_v070, delta, explicação).

### AC2 — Execução em 5 evals + tabela preenchida
Documento `docs/qa/gate2-story91-results.md` com tabela preenchida (eval_id, contexto, score_v060, score_v070, delta, explicação).

### AC3 — Veredito Gate 2
Critério ajustado pelo @po (Story 9.1):
- Delta ≤15pt ✅
- Cada delta rastreável à nova fórmula ✅
- Direção consistente (monotônicos ↓, variados ↑ não testável sem ground truth — skip essa regra)

Se delta máximo >15pt OU sem rastreabilidade → FAIL + issue pra ajuste pesos.

---

## Tasks

- [x] T1: `ml-worker/scripts/replay_eval.py` — reconstrói WorkerSuccess de dict, chama aggregate_metrics em ambos paths
- [x] T2: Rodar em 5 evals, gerar `docs/qa/gate2-story91-results.md`
- [x] T3: Veredito + fechar Gate 2 protocol

## Change Log
| Data | Agente | Ação |
|---|---|---|
| 2026-04-17 | @dev | Story drafted + executed inline |
