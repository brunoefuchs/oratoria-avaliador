# Story 1.4: Processing Status UI & Polling

## Status
InReview

## Executor Assignment
- executor: @dev
- quality_gate: @architect
- quality_gate_tools: [lint, typecheck, test]

## Story

**As a** user,
**I want** to see the current processing status of my video in real-time,
**so that** I know the system is working and approximately how long it will take.

## Acceptance Criteria

1. Tela de processamento em `/processing/[id]` mostra etapa atual (ex: "Separando áudio e vídeo...")
2. Frontend faz polling do endpoint de status a cada 3 segundos
3. Exibe indicador visual de progresso (stepper com etapas + spinner na etapa atual)
4. Quando status muda para `completed`, redireciona automaticamente para `/report/[id]`
5. Quando status muda para `error`, exibe mensagem de erro amigável com opção de tentar novamente
6. Tela funciona em mobile (responsiva)

## CodeRabbit Integration

> **CodeRabbit Integration**: Disabled — Greenfield project, CodeRabbit não configurado.

## Tasks / Subtasks

- [x] Task 1: Criar componente ProcessingStatus (AC: 1, 3)
  - [x] Stepper visual com 6 etapas e ícones diferenciados
  - [x] Spinner animado SVG na etapa atual
  - [x] Checkmark verde nas etapas concluídas
  - [x] Número cinza nas etapas pendentes
  - [x] Tailwind puro (sem shadcn/ui — não necessário para este componente)
- [x] Task 2: Implementar polling hook (AC: 2)
  - [x] `useEvaluationStatus(id)` com fetch a cada 3s
  - [x] Retorna `{data, isLoading, error}` com cleanup no unmount
  - [x] Para polling quando `completed`, `analyzed` ou `error`
- [x] Task 3: Criar página de processamento (AC: 1, 4, 5, 6)
  - [x] `/processing/[id]` com polling hook + ProcessingStatus
  - [x] Redirect automático para `/report/[id]` quando completo
  - [x] Tela de erro com botão "Enviar outro video" (redireciona para /)
  - [x] Loading state enquanto primeiro fetch
  - [x] Responsivo com flex/padding
- [x] Task 4: Criar página placeholder do relatório (AC: 4)
  - [x] `/report/[id]` com placeholder "Relatorio em construcao"
  - [x] Mostra evaluation ID
- [ ] Task 5: Testes (AC: 2, 4, 5)
  - [ ] Deferred — requer Vitest + testing-library setup

## Dev Notes

### Substatus Steps
[Source: architecture.md#api-specification]

Os substatus seguem esta sequência no pipeline:

| Step | Substatus | Label na UI |
|------|-----------|-------------|
| 1 | `splitting` | Separando áudio e vídeo... |
| 2 | `analyzing_posture` | Analisando postura... |
| 3 | `analyzing_gesture` | Analisando gestual... |
| 4 | `analyzing_voice` | Analisando tom de voz... |
| 5 | `analyzing_fillers` | Detectando vícios de linguagem... |
| 6 | `generating_report` | Gerando relatório... |

### Endpoint de Status
[Source: architecture.md#api-specification]

```json
// GET /evaluations/{id}/status
{
  "id": "550e8400...",
  "status": "processing",
  "substatus": "analyzing_voice",
  "progress": {
    "steps_completed": 3,
    "steps_total": 6,
    "current_step": "Analisando tom de voz...",
    "estimated_remaining_seconds": 120
  }
}
```

### Frontend — Estrutura de Arquivos

```
apps/web/src/
├── app/
│   ├── page.tsx                         # Upload (Story 1.2)
│   ├── processing/
│   │   └── [id]/
│   │       └── page.tsx                 # Processing status ← ESTA STORY
│   └── report/
│       └── [id]/
│           └── page.tsx                 # Report placeholder ← ESTA STORY
├── components/
│   ├── video-uploader.tsx               # (Story 1.2)
│   └── processing-status.tsx            # ← ESTA STORY
├── hooks/
│   └── use-evaluation-status.ts         # ← ESTA STORY
└── lib/
    └── api-client.ts                    # (Story 1.2)
```

### Polling Pattern

```typescript
// Hook pattern com cleanup
function useEvaluationStatus(id: string) {
  const [data, setData] = useState(null);

  useEffect(() => {
    const interval = setInterval(async () => {
      const status = await apiClient.get(`/evaluations/${id}/status`);
      setData(status);
      if (status.status === 'completed' || status.status === 'error') {
        clearInterval(interval);
      }
    }, 3000);
    return () => clearInterval(interval);
  }, [id]);

  return data;
}
```

### Error States
[Source: architecture.md#error-handling-strategy]

| Status | UI Behavior |
|--------|-------------|
| `processing` | Stepper com progresso |
| `completed` | Redirect para `/report/[id]` |
| `error` | Mensagem de erro + botão retry |

### Dependências
- Depende de Story 1.2 (endpoint `POST /evaluations` e tabela `evaluations`)
- Depende de Story 1.3 (endpoint `GET /evaluations/{id}/status` e pipeline skeleton)

### Testing
- Vitest + @testing-library/react para componentes
- Mock do fetch para simular diferentes status de polling
- Testar: renderização do stepper, transição de status, redirect automático

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-30 | 0.1 | Story criada — YOLO mode | @sm (River) |

## Dev Agent Record

### Agent Model Used
Claude Opus 4.6 (1M context)

### Debug Log References
- Next.js build: OK — `/` (89kB), `/processing/[id]` (89.9kB), `/report/[id]` (87.9kB)

### Completion Notes List
- Usado Tailwind puro para stepper (sem shadcn/ui) — mais leve e sem dependência adicional
- Retry button redireciona para `/` (novo upload) em vez de `POST /retry` (endpoint não existe ainda)
- Hook para polling quando status é `analyzed` também (pipeline atual termina em analyzed, não completed)

### File List
- `apps/web/src/hooks/use-evaluation-status.ts` — NEW
- `apps/web/src/components/processing-status.tsx` — NEW
- `apps/web/src/app/processing/[id]/page.tsx` — NEW
- `apps/web/src/app/report/[id]/page.tsx` — NEW

## QA Results
_To be filled by QA agent_
